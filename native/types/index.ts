export enum USER_ROLE {
  USER = "user",
  FREELANCER = "freelancer",
}

export enum LOGIN_METHOD {
  PHONE = "phone",
  EMAIL = "email",
}
export enum SIGN_UP_STEP {
  FORM = "form",
  OTP = "otp",
  DONE = "done",
}

