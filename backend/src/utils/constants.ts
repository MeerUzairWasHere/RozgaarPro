export const MAX_NUMBER_OF_SKILLS = 3;

export const MAX_RADIUS_KM = 5;

export const MAX_NUMBER_OF_GALLERY_IMAGES = 5; // max per upload batch
export const MAX_TOTAL_GALLERY_IMAGES = 100; // max total in gallery

export const ENUM_FIELDS: Record<string, string> = {
  status: '"FreelancerStatus"',
  role: '"Role"',
};
